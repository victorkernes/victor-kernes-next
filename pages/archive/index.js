import Page from "../../components/Page";
import Archive from "../../components/Archive"

export default () => (
  <Page>
    <section className={"Container"}>
      <Archive />
    </section>
  </Page>
);
